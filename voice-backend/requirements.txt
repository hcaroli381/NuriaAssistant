fastapi>=0.115.0
uvicorn>=0.30.6
httpx>=0.27.2
numpy>=2.1.1
sounddevice>=0.4.7
vosk>=0.3.41
# openwakeword pulls in tflite-runtime on Linux, which has no Python 3.14
# wheels. This project only uses the ONNX inference path (local .onnx model),
# so on Python 3.14 install it manually once:
#   pip install --no-deps openwakeword==0.6.0 onnxruntime scikit-learn scipy
# (main.py auto-downloads openwakeword's feature-extraction ONNX models on first use.)
openwakeword>=0.6.0
# Natural neural TTS. Without it the backend falls back to espeak-ng (robotic).
piper-tts>=1.2.0
pydantic>=2.9.2
python-dotenv>=1.0.1
